[package]
name = "spawnado"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2"

[profile.release]
lto = "thin"
codegen-units = 1
debug = "line-tables-only"
